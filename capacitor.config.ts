//ll

import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.emojisdartverein.app",
  appName: "EMD Vereinsapp",
  webDir: "www",
  server: {
    url: "https://emojisdartverein.netlify.app",
  },
}

export default config

